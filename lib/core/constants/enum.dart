enum TextSizes { small, medium, large }

enum OrderStatus { proccesing, shipped, deliverd, cancel }

enum PaymentMehod { paypal, googlePay, applePay, visa, masterCard, creditCard }

enum ProductType {single, variable}